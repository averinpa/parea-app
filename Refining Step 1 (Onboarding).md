Prompt для Claude: Refining Step 1 (Onboarding)
Контекст: Я дорабатываю первый экран регистрации (Step 1 of 5) для приложения Parea. Мне нужно улучшить UX и логику взаимодействия на основе текущего дизайна.

Твоя задача — обновить код экрана, внедрив следующие изменения:

1. Логика выбора Gender (Кнопки):

Состояние: Реализуй выбор только одного варианта (Male, Female или Non-binary).

Визуальный отклик: При выборе кнопка должна плавно (animation) менять стиль: фон становится ярко-фиолетовым (как основная кнопка Continue), а текст — белым.

Неактивное состояние: Остальные кнопки должны иметь светло-голубой/серый фон и темный текст (как на исходном скриншоте).

2. Улучшенный ввод Age:

Ограничение: Разреши ввод только цифр (keyboardType="number-pad").

Валидация: Если введенный возраст меньше 18, выводи под полем аккуратную красную надпись: "You must be 18 or older to use Parea".

Авто-закрытие: Клавиатура должна скрываться автоматически после ввода двух цифр.

3. Умная кнопка Continue:

Состояние Disabled: Кнопка "Continue" должна быть полупрозрачной (opacity: 0.5) и не нажиматься, пока не выполнены 3 условия:

В поле "Name" введено хотя бы 2 символа.

Выбран один из вариантов "Gender".

Введен возраст от 18 до 99.

Активное состояние: При выполнении условий кнопка становится яркой и активной.

4. Работа с клавиатурой (Layout):

Оберни контент в KeyboardAvoidingView (или аналог), чтобы при появлении клавиатуры для ввода имени или возраста кнопка "Continue" не перекрывалась, а всё содержимое экрана аккуратно сдвигалось вверх.

5. Мелкие правки UI:

Соблюдай радиус скругления углов (12-16px).

Добавь под заголовком "Tell us about yourself" текст: "This info will be visible on your profile" (светло-серым цветом).

Убедись, что индикатор прогресса (Step 1 of 5) соответствует макету.

Технический стек: Используй React Native / Expo (или укажи свой фреймворк). Пиши чистый, модульный код.